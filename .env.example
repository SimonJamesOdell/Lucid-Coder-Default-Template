# Copy to .env for local development
# In production, these must be set with secure values.
JWT_SECRET=replace-with-a-long-random-secret
CORS_ORIGINS=http://localhost:5201,http://127.0.0.1:5201
PORT=3001
NODE_ENV=development
