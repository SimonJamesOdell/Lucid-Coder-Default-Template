{
  "id": "contract_auth",
  "type": "contract",
  "description": "Canonical auth contract shared by frontend and backend LLM layers.",
  "baseUrl": "__DYNAMIC__",
  "login": {
    "method": "POST",
    "route": "/api/login",
    "inputs": ["email", "password"],
    "outputs": ["token", "user"]
  },
  "signup": {
    "method": "POST",
    "route": "/api/signup",
    "inputs": ["name", "email", "password"],
    "outputs": ["token", "user"]
  },
  "llm_notes": "Compiler validates this contract against llm_src_backend/endpoints/*.json and fails build on mismatch. baseUrl is resolved at runtime: if window.API_BASE_URL is set, use it; else use window.location.origin with port 3001; else fallback to http://localhost:3001."
}
